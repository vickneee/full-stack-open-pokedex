// // eslint-disable-next-line no-undef
// const { test, describe, expect } = require('@playwright/test')
//
// describe('Pokedex', () => {
//   test('pokemon page can be opened and navigated to page where is word chorofyll', async ({ page }) => {
//     await page.goto('')
//     await page.getByText('bulbasaur').click()
//     await expect(page.getByText('chlorophyll')).toBeVisible()
//     await page.getByText('Next').click()
//     await expect(page.getByText('ivysaur')).toBeVisible()
//     await page.getByText('Next').click()
//     await expect(page.getByText('venusaur')).toBeVisible()
//     await page.getByText('Previous').click()
//     await expect(page.getByText('ivysaur')).toBeVisible()
//   })
// })
